// Increment when persisted render metadata changes even if the SVG renderer does not.
// Version 2 stores Typst's physical page list instead of one aggregate document page.
export const FOLEA_RENDERER_VERSION = '2';
export const TYPST_COMPILER_VERSION_TAG = 'typst.ts@0.7.0';
